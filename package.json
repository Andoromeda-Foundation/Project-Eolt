{
  "name": "project-eolt",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"http-server -p 5000 --register=http://localhost:5000"
  },
  "author": "Dawn",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^0.11.1"
  }
}
